# A1.4 — Authentication E2E Production Certification

Baseline parent: `a91306e1ecbbae4d713cd3c06ee971f825225375`

## Production preflight (read-only)

- Canonical global roles present: admin, artisan, client.
- Production counts observed at certification time: 1 admin, 15 artisans, 32 clients.
- Active Enterprise memberships: 2.
- Membership distribution: admin 1, client 1, artisan 0.
- Active Enterprise accounts: 2.

No personal identifiers were read for this inventory.

## Certified invariants

1. Gateway Client / Artisan / Enterprise is presentation context only.
2. `public.users.role` remains global role authority.
3. Client → Client dashboard when no Enterprise membership exists.
4. Artisan → Artisan dashboard when canonical artisan ownership exists; otherwise onboarding remains mandatory.
5. Admin → Admin dashboard when no Enterprise membership exists.
6. Any canonical global role with a verified active Enterprise membership routes through Workspace Selector.
7. Workspace Selector builds Enterprise links only from canonical resolver fields.
8. Enterprise Guard requires the requested Enterprise membership; global Admin is not an Enterprise bypass.
9. Invalid/stale Enterprise shapes do not create an Enterprise destination.
10. Logout clears canonical browser identity and Supabase session state.
11. Password recovery preserves the existing split: email reset through Supabase; phone-first accounts use the existing WhatsApp support path.
12. No A1.4 test mutates Production business/auth data.

## Scope

A1.4 adds certification artifacts only. No SQL, RLS, Auth schema, membership, user, artisan, Enterprise account, password, secret or Production data mutation is part of this certification.
