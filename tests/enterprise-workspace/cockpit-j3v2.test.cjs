const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');const r=path.join(__dirname,'../..');
test('J3V2-01 router is observer and data free',()=>{const j=fs.readFileSync(path.join(r,'js/fixeo-enterprise-cockpit.js'),'utf8');assert.doesNotMatch(j,/MutationObserver|IntersectionObserver|\.rpc\(|\.from\(|fetch\(|hidden\s*=/);assert.match(j,/data-j3-view/);});
test('J3V2-02 canonical auth workspace remains untouched',()=>{const h=fs.readFileSync(path.join(r,'dashboard-enterprise.html'),'utf8');assert.match(h,/<section id="enterprise-workspace"[^>]+hidden>/);assert.match(h,/fixeo-enterprise-workspace\.js\?v=block-g1/);});
test('J3V2-03 all domain views route native modules',()=>{const j=fs.readFileSync(path.join(r,'js/fixeo-enterprise-cockpit.js'),'utf8');for(const id of ['enterprise-control-tower','enterprise-sites','enterprise-workforce-module','enterprise-preventive-maintenance','enterprise-governance-module','enterprise-finance-module','enterprise-reporting','enterprise-team','enterprise-rafi-module'])assert.match(j,new RegExp(id));});