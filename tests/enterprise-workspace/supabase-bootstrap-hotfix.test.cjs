const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');const r=path.join(__dirname,'../..'),rd=p=>fs.readFileSync(path.join(r,p),'utf8');
test('enterprise preloads Supabase SDK before local client',()=>{const h=rd('dashboard-enterprise.html');const sdk=h.indexOf('@supabase/supabase-js@2/dist/umd/supabase.js'),client=h.indexOf('js/supabase-client.js?v=bootstrap2');assert.ok(sdk>=0&&client>sdk);});
test('client bootstrap is bounded and fail closed',()=>{const s=rd('js/supabase-client.js');assert.match(s,/SDK_TIMEOUT_MS = 8000/);assert.match(s,/SUPABASE_SDK_TIMEOUT/);assert.doesNotMatch(s,/resolve\(\{ configured: true, client: null \}\)/);});
test('auth and enterprise guards remain loaded after client',()=>{const h=rd('dashboard-enterprise.html');const c=h.indexOf('js/supabase-client.js?v=bootstrap2'),a=h.indexOf('js/fixeo-auth-resolver.js'),g=h.indexOf('js/fixeo-enterprise-guard.js');assert.ok(c<a&&a<g);});